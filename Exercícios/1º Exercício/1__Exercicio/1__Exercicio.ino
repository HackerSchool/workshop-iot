/*
 * Workshop IOT -  HackerSchool
 * 1º Exercício - Colocar LED incluído na placa a piscar

  Biblioteca:
  // Inicializa o pino como input, output e pull-up
  pinMode(LED, TIPE);     

  // Define um valor lógico nos pinos digitais
  digitalWrite(PIN, Valor lógico);

  // Para o microcontrolador durante x milissegundos
  delay(milissegundos);                      

*/

// Função de inicialização
void setup() {
  // Inicializa o pino LED_BUILTIN como output
  
}

// Função que corre para sempre
void loop() {
  // Liga o LED (Devido às propriedades da placa ESP-12, colocar o nível lógica a '0', liga o LED)
  
  // Espera 1 segundo
  
  // Desliga o LED (agora será necessário colocar o nível lógica a '1' para desligar o LED)
  
  // Espera 2 segundos

  
}
